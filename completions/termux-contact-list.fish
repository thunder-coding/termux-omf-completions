# Completions for `termux-contact-list`
# Author: Yaksh Bariya <yakshbari4@gmail.com>

complete -c termux-contact-list -f -d "Get device contact in JSON formar"
complete -c termux-contact-list -s h -d "Get help for termux-contact-list"
