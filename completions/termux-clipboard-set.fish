# Completions for `termux-clipboard-set`
# Author: Yaksh Bariya <yakshbari4@gmail.com>

complete -c termux-clipboard-set -f -d "Modify text in device clipboard from terminal"
complete -c termux-clipboard-set -s h -d "Get help for termux-clipboard-set"
